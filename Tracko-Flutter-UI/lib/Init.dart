class InitializeApp {
  static Future initialize() async {
    // App is now fully backend-driven
    // No local database or mobile ads initialization needed
    // All data is managed through backend APIs
  }
}
