name: Pull Action on Trako

on:
  pull_request:
    branches:
      - main

jobs:
  backend-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Run backend tests
        run: mvn verify -Dspring.profiles.active=test
        working-directory: backend

  cli-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: maven

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov pytest-mock psutil requests
          pip install ./sdk
          pip install ./cli

      - name: Run CLI tests with backend
        run: python cli/run_cli_test.py

      - name: Upload backend log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cli-backend-log
          path: cli/tests/backend.log
          if-no-files-found: warn

  docker-build:
    runs-on: ubuntu-latest
    needs: [backend-test, cli-test]

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t tracko:${{ github.sha }} .
